# 🔍 SKILL: /index-codebase

## Purpose
Refresh project context using `context7` MCP.

## Workflow
1. Identify dependencies without local docs.
2. Trigger `context7` via MCP protocol.
3. Fetch latest docs/API references.
4. Update `.ai/context7-index.md` (or similar).
5. Log usage in `memory.md`.

## Constraint
Requires `TASKIFY_MCP_CONTEXT7=true`.
