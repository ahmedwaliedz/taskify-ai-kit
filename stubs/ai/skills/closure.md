# ✅ SKILL: /closure

## Purpose
Finalize feature and clean up.

## Workflow
1. Run full test suite.
2. Run Laravel Pint (formatting).
3. Update `PROJECT_CONTEXT.md` with feature summary.
4. Compress `memory.md` (via `/compress-memory`).
5. Merge feature branch (squash).
6. Archive feature context.

## Output
`✅ Feature ready`
