# 🏗️ SKILL: /plan

## Purpose
Design the technical implementation.

## Workflow
1. Detect Laravel version.
2. Create/Switch to `feat/{slug}` branch.
3. Design DB Schema (Migrations).
4. Define API Contracts / Service interfaces.
5. Apply "Service 2-of-4" rule.
6. Create `features/{slug}/plan.md`.

## Output
`⏸️ Awaiting @approve`
